---
interface Props {
  adSlot: string;
  label: string;
  minHeight?: number;
}

const { adSlot, label, minHeight = 280 } = Astro.props;
---

<div
  class="ad-host"
  data-ad-slot={adSlot}
  data-ad-label={label}
  data-ad-min-height={String(minHeight)}
></div>

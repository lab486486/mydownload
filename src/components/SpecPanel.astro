---
import { downloadLabel, type SoftwareEntry } from '../lib/software';

interface Props {
  entry: SoftwareEntry;
}

const { data } = Astro.props.entry;
---

<aside class="panel spec-panel">
  {data.icon ? (
    <img class="icon" src={data.icon} alt="" width="56" height="56" />
  ) : (
    <span class="icon fallback">{data.name.slice(0, 1)}</span>
  )}
  <strong>{data.name}</strong>
  <p class="muted spec-panel-note">공식 경로로 이동합니다.</p>
  {data.downloads.length > 0 && (
    <div class="download-list">
      {data.downloads.map((item) => (
        <a href={item.url} rel="noopener noreferrer" target="_blank">
          {downloadLabel(item)}
        </a>
      ))}
    </div>
  )}
</aside>
